# LEGACY ONLY: the current app uses config.json + secrets.env and per-record JSON.
# Copy this file to the deployed application's .env. Both the development and
# packaged applications must use this exact shared database file.
DATABASE_URL=sqlite:///\\landisk-87a5d6\disk1\KY検出システムデータ\ky_analysis_history.sqlite3

# UNC path. Grant the users read/write access to this folder. Do not use a
# mapped drive letter.
PHOTO_STORAGE_DIR=\\landisk-87a5d6\disk1\KY検出システムデータ\photos

# Used only until a future login integration supplies the authenticated user.
# Leave blank to record the Windows sign-in name automatically.
DEFAULT_CREATED_BY=
